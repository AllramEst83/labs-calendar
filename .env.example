# Copy to .env for local `netlify dev` — set the same values in the Netlify UI for production.
AUTH_PIN=123456
JWT_SECRET=replace-with-a-long-random-secret
CORS_ALLOWED_ORIGINS=http://localhost:8888,http://localhost:5173,http://127.0.0.1:8888,http://127.0.0.1:5173,https://labs-calendar.netlify.app
